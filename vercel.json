{
  "framework": "nextjs",
  "buildCommand": "pnpm build",
  "installCommand": "pnpm install --frozen-lockfile",
  "crons": [
    { "path": "/api/cron/digest?slot=am", "schedule": "30 7 * * *" },
    { "path": "/api/cron/digest?slot=pm", "schedule": "0 15 * * *" }
  ]
}
